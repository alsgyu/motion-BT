diff --git a/INHA-Player/src/brain/include/kick.h b/INHA-Player/src/brain/include/kick.h
index c4a6d291..2abb862c 100644
--- a/INHA-Player/src/brain/include/kick.h
+++ b/INHA-Player/src/brain/include/kick.h
@@ -179,2 +179,3 @@ private:
     bool _headReacquireTimeoutLogged = false;
+    bool _headReacquireUsedLastKnown = false;
     double _decelDurationMs = 500.0;
diff --git a/INHA-Player/src/brain/include/robot_client.h b/INHA-Player/src/brain/include/robot_client.h
index 092fadc9..3aa4ee24 100644
--- a/INHA-Player/src/brain/include/robot_client.h
+++ b/INHA-Player/src/brain/include/robot_client.h
@@ -166,2 +166,3 @@ private:
     int publishCustomMotionHead(double pitch, double yaw);
+    int stopMotionForModeChange();
     rclcpp::Publisher<booster_msgs::msg::RpcReqMsg>::SharedPtr publisher;
diff --git a/INHA-Player/src/brain/src/kick.cpp b/INHA-Player/src/brain/src/kick.cpp
index 06c72261..12c33ca1 100644
--- a/INHA-Player/src/brain/src/kick.cpp
+++ b/INHA-Player/src/brain/src/kick.cpp
@@ -724,2 +724,3 @@ void RLVisionKick::startVisionKick()
             _soccerModeConfirmedTime = rclcpp::Time(0, 0, RCL_ROS_TIME);
+            resetSoccerHeadReacquire();
             brain->tree->setEntry<bool>("visual_kick_command_sent", false);
@@ -787,2 +788,3 @@ void RLVisionKick::resetSoccerHeadReacquire()
     _headReacquireTimeoutLogged = false;
+    _headReacquireUsedLastKnown = false;
 }
@@ -800,2 +802,14 @@ void RLVisionKick::stepSoccerHeadReacquire()
 
+    if (!_headReacquireUsedLastKnown) {
+        _headReacquireUsedLastKnown = true;
+        const bool hasLastKnownBall = brain->tree->getEntry<bool>("ball_location_known");
+        double pitch = brain->data->filterball.pitchToRobot;
+        double yaw = brain->data->filterball.yawToRobot;
+        if (hasLastKnownBall && std::isfinite(pitch) && pitch > 0.0 && std::isfinite(yaw)) {
+            brain->client->moveHead(std::max(pitch, 0.55), yaw);
+            _lastHeadReacquireTime = brain->get_clock()->now();
+            return;
+        }
+    }
+
     const bool shouldSend =
diff --git a/INHA-Player/src/brain/src/robot_client.cpp b/INHA-Player/src/brain/src/robot_client.cpp
index 2198dafd..7a29ddfd 100644
--- a/INHA-Player/src/brain/src/robot_client.cpp
+++ b/INHA-Player/src/brain/src/robot_client.cpp
@@ -113,3 +113,6 @@ int RobotClient::changeRobocupMode()
 
-    return call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kSoccer));
+    stopMotionForModeChange();
+    const int ret = call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kSoccer));
+    stopMotionForModeChange();
+    return ret;
 
@@ -121,3 +124,6 @@ int RobotClient::changeCustomMode()
 
-    return call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kCustom));
+    stopMotionForModeChange();
+    const int ret = call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kCustom));
+    stopMotionForModeChange();
+    return ret;
 }
@@ -130,3 +136,6 @@ int RobotClient::changeWalkingMode()
     const auto walkingMode = static_cast<booster::robot::RobotMode>(2);
-    return call(booster_interface::CreateChangeModeMsg(walkingMode));
+    stopMotionForModeChange();
+    const int ret = call(booster_interface::CreateChangeModeMsg(walkingMode));
+    stopMotionForModeChange();
+    return ret;
 }
@@ -149,3 +158,6 @@ int RobotClient::enterDamping()
 {
-    return call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kDamping));
+    stopMotionForModeChange();
+    const int ret = call(booster_interface::CreateChangeModeMsg(booster::robot::RobotMode::kDamping));
+    stopMotionForModeChange();
+    return ret;
 }
@@ -262,2 +274,16 @@ int RobotClient::publishCustomMotionVelocity(double x, double y, double theta)
 
+int RobotClient::stopMotionForModeChange()
+{
+    _vx = 0.0;
+    _vy = 0.0;
+    _vtheta = 0.0;
+    _lastCmdTime = brain->get_clock()->now();
+
+    if (brain->config && brain->config->customMotionEnable) {
+        publishCustomMotionVelocity(0.0, 0.0, 0.0);
+    }
+
+    return call(booster_interface::CreateMoveMsg(0.0, 0.0, 0.0));
+}
+
 int RobotClient::publishCustomMotionHead(double pitch, double yaw)
